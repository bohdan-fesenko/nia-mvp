## Agents tools
Ok. Let's change anything according what i will say next:
1. Main agent is the conversationsal agent that is always is the main interface for commication with user (front-end). He has shortterm memory of last 30 messages from user (configurable) and his task is to keep conversation with a user and transfer his requests to the specific chat session when needed (by inserting messages into the chats so they read this before their next pass (read means add to chat history as last message at next agent execution). this main conversational agent can also keep conversation with user because he has tools like speak_user, he can also stop immediately some agent by interrupting it's streaming api (we should handle those cases too so user can interrupt if something is going wrong). This main conversational agent can create new chats if user has some task ouside of clarify current chats or interact with them. Because for anything from little heavy this agent should create a new chat session (ofc if user is not speaking with current chats) because chat session (i mean execution agent) has much more capabilities, like long term memory, graph memory (agent will be provideed with initial context and neo4j structure by some initial vector and keyword based search, and then agent can create some queries using specific llm tool to gather more information in the process of satisfying user request. And also, this conversation agent is conversational because he will have both text and voice input (using livekit).
2. So each chat session will handle heavy staff. This will be also Pydantic-AI agents with tools calling that will be have some tools I'll define some of them now and some will add later. You can also add a additional section with tools you propose. What tools i want? I want to have tools that mimic real neural artificial intelligence asisstant for humans. He can retrieve information about user from graph database, he can store information in the database. He can change, create, or anything do with documents(pages) and folders and change them on the user behalf or during some task completion when decide to do this. Becides tools to read, write , replace, search etc (like in Cline work with code ) tools to work with documents and blocks inside them, and OF COURSE their relations (searching through them and also defining them for all new data you create into a database), I want to have a tools to be searchable from a database as RAG and to be added on-the-fly to agent by their relevance. Because we can also have tools for interacting with browser using browser-use and computer shell terminal to execute code for example, and we'll add those tools when needed. 
Also there are a fact that every model has it's context_window size and in our context or brain mimicing (using graph data and searching & storing) we need to have this parameter defined (this will be 200k tokens fro the first time) and model should keep the context updated when reach near the limits, so the model have also tools to forget some information to remove it from it's current context_window. Agent can also change it's temperature to change it's creativity level for ceration situations using tool calling, for example for creting writing or brainstorming, and also start force thinking process using reasoning models .This all means agent can control it's execution for the best of the user request and mimic brain.

Let's modify docs according to this vision i just providede. 


## Clarification about User Flow of user Chat Session and Page to di
some additional comments that are aligned with current concept. User has chat sessions and he should have some interactive (with notifications) navbar in the center lower of the page to interact with them and select them to view current chat of each agents. And on each created page with chat session we will display some of the logs from database about agent process, for example documents diff when agent change some docuemnts or folders, also agent can ask user something about the documents or ask for approval and then user can see this notification (agent created an entry and front-end somehow tracked this and displayed) and then when user select this chat session / page he see current latest information we display to him, e.g. latest document diff (because thinking about what call to use we don't show to user for example), and also a question to user if such arrived though agent's tool calling. please adapt docs and tasks where needed according to this clarification.

## Clarify  about Document Diff Commit changes
do document diff we will use tool defined the same as in github ai copilot, with commit line by line and we track those diff and present it to use like in vs code ide commit changes (just save appropriate format and then those lines will be rendered as red and green at the front-end)

## DEBUG websockets. REMOVE THEM
Listen to me: why we use unrobust websockets while we have all our data stored in the database? also there is fact that ai agents (core of our product) will also work with data and change it in the database. Front-end should be connected to database means update information on the ui according to real-time changes that can occur on neo4j dataabse, that is. Websockets are unreliable, let's remove them completely!


# 
First of all, yes i want this architectural change, also look at the docs and update them first before if they need change according to your plan.
I want the best real-time solution for front-end (you can change tech stack for this!), for example sse or what is the best. the goal is that user will see updates when they are made (for example similar when i see real-time comments chat in youtube or discord or twitch, i want to have similar experience here given my other requirements!

we choosed Next Auth authentication, and created task for this @/web_app/project_docs/tasks/TASK_009_next_auth_implementation.md , but didn't implemented yet. Also i can add google oauth credentials to implement real google login so i can create my account and test like production. If implementing auth needed to be done before websocket change then tell me - you decide bext next steps!
